class Solution:
    def findWordsContaining(self, words: List[str], x: str) -> List[int]:
        cnt=[]
        for i in range(len(words)):
            if x in words[i]:
                cnt.append(i)
        return cnt
